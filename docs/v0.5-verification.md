# 0.5 verification — 2026-09-08

Targeted domain checks ran before the full suite. Existing tests were updated
only where 0.5 intentionally changes XP, minute recovery, fatigue effects, source
availability or catalog counts; original checks remain present.

`tools/test.ps1 -Visual`: **634 passed, 0 failed** across the domain suites.
Two-process restart write/read: PASS. Typography, collaboration, career final,
0.5 UI and visual smoke: all PASS. `python tests/test_streamer_import.py`:
**3 passed** (offline mocked API). Godot logs contain no script errors/warnings.

Logs: `build/checks/v05-full.log`, `build/checks/v05-systems.log`.
Screenshots: `build/checks/typography/`.

Reviewed the room, live UI, kitchen, location selector, locked/partial achievement
tree, real collaboration cards, missing-source topics and available FAIL topic.
Text remains readable at 540x960, UI stays within its bounds, and Kitchen neither
overlays the modal nor duplicates the room. Ten location transitions pass the
single-child and containment checks. Nearest filtering is retained.

All twelve required 0.5 screenshot filenames are generated by
`tests/v05_ui_smoke.gd`; the earlier offline/live/moves/games/events/upgrades
screenshots remain generated by the typography smoke.

**Open acceptance item:** YOUNG/SUCCESSFUL assets still need background cleanup
and connection. The generator returned a painted checkerboard instead of alpha.
The user's explicit permission for programmatic image editing is pending.
Consequently the three appearance screenshots currently show the same CURRENT
placeholder; they are not evidence that the three visual stages are complete.
See `v0.5-assets.md` for the generated source and exact prompt.

No Android SDK/device verification, runtime networking or main-branch merge.
