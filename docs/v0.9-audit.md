# v0.9 targeted audit

Base: main a9188c05ef1c76cb999e5db8815797ae09b5c73c, matches requested base. One agent, one feature branch.

| Issue | Reproduced | Root cause | Fix | Regression test |
|---|---|---|---|---|
| Click approaches +8 hype | Confirmed formula | Equipment, level mastery and efficiency multiply gain | Fixed 0.7; XP independent | v09_systems boundaries |
| Weak hype value | Confirmed coefficients | Old audience curve and linear organic quality | Central interpolated modifiers | Anchors, monotonicity, calculator |
| Missing progression gates | Confirmed purchases | Only definition unlock/career tier checked | Next upgrade level and resource home requirement | Purchases and old save |
| Monitor above modal | Confirmed hierarchy | RightMonitor z=1 shares canvas with modal z=0 | ModalCanvas layer 100 above gameplay canvas 0 | v09_ui_smoke |
| Cooking publish disabled at 97 fatigue | Confirmed UI and service | Both reject fatigue plus cost over 100 | Shared availability, clamp fatigue without rejecting publication | 97+8 regression |
| Cooking material lifecycle | Reset loss not reproduced in base | Source already created before reset, but uses mutable selection | Capture session format and sampled hype | Four formats, summary and save |
| Source metadata migration | Confirmed dictionary archive | No optional hype validation | Validate 0..100; old missing field means neutral zero | Save regression |

Historical balance assertions are updated only where 0.9 changes their contract; unrelated coverage is retained.

Verification: all targeted systems and the complete verification runner passed.
The UI smoke covers ten modal views (including the collab offer), plus Cooking
97% publication through the real UI. Theme is explicitly bound on the modal root:
CanvasLayer interrupts theme inheritance. Gameplay uses canvas 0 (monitor z=1),
regular layout UI z=10, and modal canvas 100. Existing overlay input blocking is retained.
