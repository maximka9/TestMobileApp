# Проверка 0.4.0 — этапы 9–10

Команда: `powershell -ExecutionPolicy Bypass -File tools/test.ps1 -Visual`.

Полный прогон: 570 проверок домена, сохранений и интеграции, 0 ошибок.
Отдельно прошли restart write/read и четыре набора UI/visual smoke:
typography, collaboration, career final, visual.

Миграция проверяется по схеме из базового коммита 71f7d04 (v0.3,
версия файла сохранения 1). Текущая версия схемы — 8. Проверены повторные
JSON-загрузки всех постоянных полей после готовки с косплеем, коллаборации,
публикации ролика, открытия достижений, покупки аквариума и переезда.

Исправлены отклонение истории с бонусом свежести выше 1, накопление
дубликатов жилья, уведомление очереди сохранения после действий в модальных
окнах, недостижимость SLAY KING, выход области атласа за границы изображения,
отсутствие карьерной внешности на кухне и перекрытие аквариума фоном.

Скриншоты генерируются в `build/checks/typography/` и каталоге visual smoke.
Проверены изображения основной комнаты, кухни, трёх карьерных вариантов,
купленного аквариума и модальных окон. Лог полного прогона:
`build/checks/full-v040.log` (локальный артефакт).

Версия проекта и метаданные экспорта: 0.4.0. APK на этом этапе не
пересобирался; проверка устройств и Android FPS в этот прогон не входит.
