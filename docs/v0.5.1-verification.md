# SASAclicker 0.5.1 stabilization

## Audit and lineage

Base `cc2ea43`, clean working tree, origin fetched/pruned before changes.
All five consecutive `merge-base --is-ancestor` checks returned **0**:
main → MVP → visual 0.2 → typography 0.3 → career 0.4 → career fixes 0.5.
The latest feature contained 14 commits after main, including all prior stages.
No intermediate development branch or manual chain of merges is needed.

Milestones verified from historical project.godot and commit diffs:
0.1 `51129f0`, 0.2 `9d77e92`, 0.3 typography `71f7d04`, 0.4 `d802964`.
The 0.3 commit left a stale 0.2 version string; its actual font/theme/scene changes
identify the milestone, and its annotated tag explicitly records that fact.

## Confirmed defects and fixes

- All career tiers still used CURRENT. Connected transparent YOUNG/SUCCESSFUL
  with the same face, keeping CURRENT and sharing the mapping across locations.
- Achievement nodes did not distinguish locked descendants from available nodes;
  missing parents and cycles were not validated. Added both validation and UI states.
- Catalog accepted case variants of one login and only checked date length.
  Added identity/calendar validation, external-ID fields and importer rename handling.
- README still described schema 1, fictional production streamers and old rendering.
  Replaced it with current run/test/features/persistence information.
- Old unused generic career/kitchen images were still exported. Searches across
  source, scenes and resources confirmed zero references before deletion. Kitchen
  reduced from 1454×1082/1.46 MB to 672×500/384 KB; career sheet is 256×128/47 KB.

## Verification

`tools/verify.ps1 -Targeted`: PASS, all process exit codes zero.
`-NegativeControl`: exit **1**, controlled FAIL detected; temporary script removed.
Historical serializers produced committed schema 1/8/9 fixtures, then migration
was checked repeatedly for idempotence and preserved progress.

Location smoke performs 20 complete round trips (40 replacements), tests one
click per input and one signal connection, checks old scene weak references,
character count, retained nodes/timers and orphan count. All pass.
Appearance alpha, three distinct textures and same texture in both rooms pass.
Every achievement node, including SLAY KING, is reachable at 360×640 and 390×844.
Screenshots are in `build/checks/typography/`.

Clock checks include 59/60/119/120 seconds, rollback, day/month/year intervals,
no negative fatigue and interrupted-stream exclusion. Saved interrupted streams
reset to OFFLINE without completion rewards or recovery for the closed interval;
normal recovery starts after loading. Hype/viewer modifiers remain domain-only.
The 0.5 source/FAIL/XP regression suite remains enabled.

Catalog requirement is explicitly **400+ verified profiles**, with 473 records.
External IDs absent from the public source stay empty; no IDs are invented.
Known external IDs and case-insensitive logins must be unique. Authenticated
Helix import is developer-only and was tested offline with mocked pages.
Runtime has no HTTP/socket/network client and loads the catalog from bundled JSON.
Collaboration UI runs from that local snapshot. System-wide network disconnect
was not performed (no administrator privilege); no user's network settings changed.

Physical Android verification remains deferred. No user save files were touched.
Feature full verification: **1157 Godot assertions passed, 0 failed; 4 Python
tests passed; six UI/visual smokes and two-process restart PASS**. Every child
process exited 0; verify exited 0. Log: `build/checks/v051-full.log`.
Main verification and a fresh-clone import/start check are required before branch
cleanup; their logs use `v051-main.log` and `v051-clone.log` in build/checks.
